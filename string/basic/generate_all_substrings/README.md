# Generate All Substrings

This program generates all possible substrings of a given string.

## Algorithm
- Use nested loops to generate all substrings
- Outer loop for starting position, inner loop for ending position
- Extract substring using string slicing

## Time Complexity
O(n^2) where n is the length of the string

## Space Complexity
O(n^2) - To store all substrings